env:
  global:
    - LANG:tr_TR.UTF-8

language: php

php:
    - 7.0

before_script:
    - phpenv rehash
    - composer install

script:
    - mkdir -p build/logs
    - phpunit --coverage-clover build/logs/clover.xml -c app

after_success:
    - bin/coveralls -v